  unit uTelaPrincipal;

  interface

  uses
    Winapi.Windows, Winapi.Messages, System.SysUtils, System.Variants, System.Classes, Vcl.Graphics,
    Vcl.Controls, Vcl.Forms, Vcl.Dialogs, Vcl.Menus, JvMenus;

  type
    TfrmPrincipal = class(TForm)
      mnPrincipal: TJvMainMenu;
      CADASTROS1: TMenuItem;
      USUARIOS1: TMenuItem;
      Projetos1: TMenuItem;
      AREFAS1: TMenuItem;
      RELATRIOS1: TMenuItem;
      DASHBOARD1: TMenuItem;
    SAIR1: TMenuItem;
    SISTEMA1: TMenuItem;
    SOBRE1: TMenuItem;
      procedure USUARIOS1Click(Sender: TObject);
      procedure Projetos1Click(Sender: TObject);
      procedure AREFAS1Click(Sender: TObject);
      procedure DASHBOARD1Click(Sender: TObject);
    procedure SAIR1Click(Sender: TObject);

    private
      { Private declarations }
    public
      { Public declarations }
    end;

  var
    frmPrincipal: TfrmPrincipal;

  implementation

  {$R *.dfm}

  uses uCadColaboradores, uCadProjeto, uCadTarefas, uTelaDash;



  procedure TfrmPrincipal.AREFAS1Click(Sender: TObject);
   var
      FormTarefa : TfrmCadTarefa;
  begin

      FormTarefa := TfrmCadTarefa.Create(Self);

      try
          FormTarefa.ShowModal;
        finally
          FormTarefa.Free;
      end;

  end;



  procedure TfrmPrincipal.DASHBOARD1Click(Sender: TObject);
  var
    FormDash:TfrmDash;
  begin
      FormDash := TfrmDash.Create(Self);
      try
         FormDash.ShowModal;
        finally
         FormDash.Free;
      end;
  end;

  procedure TfrmPrincipal.Projetos1Click(Sender: TObject);
  var
    FormProjeto : TfrmCadProjeto;

  begin

    FormProjeto := TfrmCadProjeto.Create(Self);

    try

       FormProjeto.ShowModal;

     finally

       FormProjeto.Free;

    end;



  end;

procedure TfrmPrincipal.SAIR1Click(Sender: TObject);
begin
  if MessageDlg('Deseja realmente sair do sistema?', mtConfirmation, [mbYes, mbNo], 0) = mrYes then
  begin
    Application.Terminate;
  end;
end;

procedure TfrmPrincipal.USUARIOS1Click(Sender: TObject);
   var
    FormColab : TformColaboradores;

  begin
    FormColab := TformColaboradores.Create(Self);
     try
        // 2. Exibe a tela em modo modal
           FormColab.ShowModal;
     finally
        // 3. Garante a liberação da memória ao fechar a tela
       FormColab.Free;
    end;
  end;
  end.
